//= require jquery3
//= require popper
//= require bootstrap-sprockets

$(function(event){
    
    var interest = 6;
    $('#forward' + interest).on('click', function (event) {
        $('#slide' + interest).removeClass("shift-back");
        $('#slide' + interest).addClass("shift-slide");
        interest = interest - 1;
        if(interest == -1){
            for(interest = 6; interest > -1; interest--){
                $('#slide' + interest).removeClass("shift-slide");
                $('#slide' + interest).addClass("shift-back");
            }
            interest = 6;
        }
    });
});
